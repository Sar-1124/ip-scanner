<Window x:Class="IPScanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IP Scanner" Height="700" Width="1000"
        Background="#0D1117"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ScanButton" TargetType="Button">
            <Setter Property="Background" Value="#00D4FF"/>
            <Setter Property="Foreground" Value="#0D1117"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00AACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#1E2A3A"/>
                                <Setter Property="Foreground" Value="#4A5568"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource ScanButton}">
            <Setter Property="Background" Value="#FF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CC2222"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#00D4FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#00D4FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridView Column Header Style -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
        </Style>

        <!-- ListView Style -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
        </Style>

        <!-- ListViewItem Style -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,1"
                                BorderBrush="#1C2128"
                                Padding="0,2">
                            <GridViewRowPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1C2128"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1F2D3D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="IP SCANNER"
                       FontSize="28" FontWeight="Bold"
                       Foreground="#00D4FF"/>
            <TextBlock Text="네트워크 장치 탐색 도구"
                       FontSize="12" Foreground="#4A5568"
                       Margin="2,4,0,0"/>
        </StackPanel>

        <!-- Input Panel -->
        <Border Grid.Row="1"
                Background="#161B22"
                CornerRadius="10"
                Padding="20"
                Margin="0,0,0,16"
                BorderBrush="#30363D"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="IP 대역"
                           Foreground="#8B949E" FontSize="12"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBox Grid.Column="1" x:Name="TxtBaseIP"
                         Style="{StaticResource InputBox}"
                         Text="192.168.1"
                         ToolTip="예: 192.168.1"/>

                <TextBlock Grid.Column="2" Text="."
                           Foreground="#4A5568" FontSize="18"
                           VerticalAlignment="Center" Margin="6,0"/>

                <TextBox Grid.Column="3" x:Name="TxtStartIP"
                         Style="{StaticResource InputBox}"
                         Text="1"
                         ToolTip="시작 범위 (1~254)"/>

                <TextBlock Grid.Column="4" Text="~"
                           Foreground="#4A5568" FontSize="16"
                           VerticalAlignment="Center" Margin="8,0"/>

                <TextBox Grid.Column="5" x:Name="TxtEndIP"
                         Style="{StaticResource InputBox}"
                         Text="254"
                         ToolTip="끝 범위 (1~254)"/>

                <TextBlock Grid.Column="6" Text="타임아웃"
                           Foreground="#8B949E" FontSize="12"
                           VerticalAlignment="Center" Margin="16,0,10,0"/>

                <TextBox Grid.Column="7" x:Name="TxtTimeout"
                         Style="{StaticResource InputBox}"
                         Text="500"
                         ToolTip="핑 타임아웃 (ms)"/>

                <TextBlock Grid.Column="8"/>

                <Button Grid.Column="9" x:Name="BtnScan"
                        Content="▶  스캔 시작"
                        Style="{StaticResource ScanButton}"
                        Click="BtnScan_Click"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="10" x:Name="BtnStop"
                        Content="■  중지"
                        Style="{StaticResource StopButton}"
                        Click="BtnStop_Click"
                        IsEnabled="False"/>
            </Grid>
        </Border>

        <!-- Progress & Stats Bar -->
        <Border Grid.Row="2"
                Background="#161B22"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,12"
                BorderBrush="#30363D"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="TxtStatus"
                                   Text="대기 중..."
                                   Foreground="#8B949E" FontSize="12"/>
                        <TextBlock Grid.Column="2" x:Name="TxtPercent"
                                   Text="0%"
                                   Foreground="#00D4FF" FontSize="12" FontWeight="Bold"/>
                    </Grid>

                    <Border Background="#0D1117" CornerRadius="4" Height="6">
                        <Border x:Name="ProgressBar" Background="#00D4FF"
                                CornerRadius="4" HorizontalAlignment="Left"
                                Width="0"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0">
                    <Border Background="#0D2137" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock x:Name="TxtAliveCount" Text="0"
                                       Foreground="#00D4FF" FontSize="20" FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="응답" Foreground="#4A5568" FontSize="10"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1A0A0A" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock x:Name="TxtDeadCount" Text="0"
                                       Foreground="#FF4444" FontSize="20" FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="무응답" Foreground="#4A5568" FontSize="10"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#0D1A0A" CornerRadius="6" Padding="12,6">
                        <StackPanel>
                            <TextBlock x:Name="TxtTotalCount" Text="0"
                                       Foreground="#3FB950" FontSize="20" FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="전체" Foreground="#4A5568" FontSize="10"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Results List -->
        <Border Grid.Row="3"
                Background="#161B22"
                CornerRadius="10"
                BorderBrush="#30363D"
                BorderThickness="1"
                Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column Header Background -->
                <Border Grid.Row="0" Background="#1C2128"
                        CornerRadius="10,10,0,0"
                        BorderBrush="#30363D" BorderThickness="0,0,0,1"/>

                <ListView Grid.Row="0" Grid.RowSpan="2"
                          x:Name="LvResults"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="  상태" Width="80"
                                            DisplayMemberBinding="{Binding StatusIcon}"/>
                            <GridViewColumn Header="IP 주소" Width="160"
                                            DisplayMemberBinding="{Binding IPAddress}"/>
                            <GridViewColumn Header="호스트명" Width="260"
                                            DisplayMemberBinding="{Binding Hostname}"/>
                            <GridViewColumn Header="응답시간" Width="100"
                                            DisplayMemberBinding="{Binding PingTime}"/>
                            <GridViewColumn Header="TTL" Width="80"
                                            DisplayMemberBinding="{Binding TTL}"/>
                        </GridView>
                    </ListView.View>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAlive}" Value="True">
                                    <Setter Property="Foreground" Value="#3FB950"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsAlive}" Value="False">
                                    <Setter Property="Foreground" Value="#4A5568"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Border>

        <!-- Footer / Export -->
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TxtFooter"
                       Grid.Column="0"
                       Text="스캔을 시작하려면 IP 대역을 입력하고 '스캔 시작'을 클릭하세요."
                       Foreground="#4A5568" FontSize="11"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="📋  결과 복사"
                    Style="{StaticResource ScanButton}"
                    Background="#21262D"
                    Foreground="#E6EDF3"
                    Click="BtnExport_Click"/>
        </Grid>
    </Grid>
</Window>
